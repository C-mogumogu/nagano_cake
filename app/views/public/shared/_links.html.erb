<%- if controller_name != 'sessions' %>
  <div class="row">
    <h4 class="col-lg-auto bg-light text-center">既に登録済みの方</h4>
  </div>
  <%= link_to "こちら", new_customer_session_path %>　からログインしてください。<br />
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <div class="row">
    <h4 class="col-lg-auto bg-light text-center">会員登録がお済みでない方</h4>
  </div>
  <%= link_to "こちら", new_customer_registration_path %>　から新規登録を行ってください。<br />
<% end %>