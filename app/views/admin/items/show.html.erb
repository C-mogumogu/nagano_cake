<h3>商品詳細</h3>

<%= image_tag @item.get_image(300,200) %>

商品名
<%= @item.name %>

商品説明
<%= @item.introduction %>

ジャンル
<%= @item.name %>

税込価格<br>
(税抜価格)

<%= @item.price %>

販売ステータス
<%= @item.sell_status %>

<%= linl_to "編集する", edit_admin_item_path(@item.id), class: "btn btn-success" %>